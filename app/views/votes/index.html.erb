<h1>Listing votes</h1>

<table>
  <thead>
    <tr>
      <th>Team</th>
      <th>Sata</th>
    </tr>
  </thead>

  <tbody>
    <%= content_tag_for(:tr, @votes) do |vote| %>
      <td><%= vote.team.name %></td>
      <td><%= vote.sata %></td>
    <% end %>
  </tbody>
</table>

<br />

<%= link_to '戻る', root_path %>
